# Rules

Do not write any code comments in this codebase. If your code needs a comment to be understood, that means the code is bad. Instead, refactor the code to be simple and readable on its own. Never add a comment for context, explanation, or for another agent. If you find a comment, remove it, unless it is a functional directive and can not be avoided.