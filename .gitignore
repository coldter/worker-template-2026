node_modules
.turbo
.alchemy
.env
.dev.vars
.vercel
.cursor
dist

coverage
.DS_Store
*.db
*.tsbuildinfo

# references
local-ref
agent-ref
*temp.md

# .sisyphus
.codex

# Production env (contains secrets)
.env.production

docs/plans
docs/superpowers
ref-doc

plans/requirements

.wrangler
.wrangler-dry-run

ref-project

# Generated OpenAPI cache (regenerated via postinstall / predev hooks)
apps/server/openapi.cache.json

# Generated wrangler runtime types (per-app, regenerated via cf-typegen)
apps/server/worker-configuration.d.ts
apps/auth/worker-configuration.d.ts

# Storybook build output
storybook-static

.tmp

# A7.4 — local TLS harness mints certs at bootstrap; never commit them.
local-harness/certs/

# A7.1 — generated worker-vars / dev-vars fragments derived from root .env.
apps/server/wrangler.jsonc.fragment.json
apps/auth/wrangler.jsonc.fragment.json
apps/admin/wrangler.jsonc.fragment.json
apps/server/.dev.vars.tenancy
apps/auth/.dev.vars.tenancy
apps/admin-ui/.env.development
apps/app/.env.development