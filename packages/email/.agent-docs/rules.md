# Email Template Rules

- Every template `.tsx` file must import React explicitly.
- Wrap templates with the shared `BaseLayout` component.
- Export new templates from `src/index.ts`.
- Keep template props typed and preview props up to date.
