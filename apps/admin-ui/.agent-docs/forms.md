# Forms

- Use React Hook Form with Zod validation.
- Keep form schemas close to the form feature/module.
- Use shared UI form primitives from `modules/ui`.
- Submit through feature mutation hooks; keep side effects outside form components.
