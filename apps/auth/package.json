{
  "name": "auth",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler types --env-interface CloudflareBindings && wrangler dev --port 8788 --inspector-port 9230",
    "deploy": "wrangler deploy --var NODE_ENV:production --var ENABLE_SIGNUP:false",
    "check-types": "tsgo --noEmit",
    "check-types:backup": "tsc --noEmit",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@repo/authorization": "workspace:*",
    "@repo/db": "workspace:*",
    "@repo/email": "workspace:*",
    "@repo/shared": "workspace:*",
    "better-auth": "^1.6.18",
    "drizzle-orm": "^1.0.0-rc.4-5d5b77c",
    "hono": "^4.12.25",
    "pg": "^8.21.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0",
    "@typescript/native-preview": "beta",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8",
    "wrangler": "^4.100.0"
  }
}
